import {expect,test} from "@playwright/test";
test.describe("responsive application shell",()=>{for(const viewport of [{name:"phone",width:390,height:844},{name:"tablet",width:768,height:1024},{name:"desktop",width:1280,height:900}]){test(`${viewport.name} has no horizontal overflow and usable controls`,async({page})=>{await page.setViewportSize({width:viewport.width,height:viewport.height});await page.goto("/login");await expect(page.getByRole("heading",{name:"Administrator login"})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBeLessThanOrEqual(viewport.width);for(const control of await page.locator("button, input").all())expect((await control.boundingBox())?.height||0).toBeGreaterThanOrEqual(40);await page.getByLabel("Email").focus();await expect(page.getByLabel("Email")).toBeFocused();expect(await page.locator("button").first().evaluate(el=>getComputedStyle(el).minHeight)).toBe("44px")})}});
