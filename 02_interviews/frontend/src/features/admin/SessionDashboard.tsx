import {FormEvent,useEffect,useState} from "react";
import {Link} from "react-router-dom";

type Session={id:string;candidate_name:string;scheduled_at:string;duration_minutes:number;state:string};
const API=import.meta.env.VITE_API_URL||"http://localhost:8000";
async function request(path:string,init:RequestInit={}){const token=localStorage.getItem("admin_token");const response=await fetch(`${API}${path}`,{...init,headers:{"content-type":"application/json",...(token?{Authorization:`Bearer ${token}`}:{}) ,...(init.headers||{})}});if(!response.ok)throw new Error((await response.json().catch(()=>({detail:"Request failed"}))).detail);return response.json()}

export function SessionDashboard(){const [items,setItems]=useState<Session[]>([]);const [error,setError]=useState("");const [name,setName]=useState("");const [when,setWhen]=useState("");const [duration,setDuration]=useState("45");async function load(){try{setItems(await request("/admin/sessions"))}catch(e){setError(String(e))}}useEffect(()=>{load()},[]);async function create(e:FormEvent){e.preventDefault();try{await request("/admin/sessions",{method:"POST",body:JSON.stringify({candidate_name:name,scheduled_at:new Date(when).toISOString(),duration_minutes:Number(duration)})});setName("");setWhen("");setError("");await load()}catch(e){setError(String(e))}}return <main className="card"><h1>Sessions</h1><form className="inline-form" onSubmit={create}><input aria-label="Candidate name" placeholder="Candidate name" value={name} onChange={e=>setName(e.target.value)} required/><input aria-label="Scheduled time" type="datetime-local" value={when} onChange={e=>setWhen(e.target.value)} required/><input aria-label="Duration" type="number" min="1" value={duration} onChange={e=>setDuration(e.target.value)} required/><button>Create</button></form>{error&&<p className="error">{error}</p>}<ul className="sessions">{items.map(x=><li key={x.id}><Link to={`/admin/sessions/${x.id}`}>{x.candidate_name}</Link><span>{x.state} · {x.duration_minutes} min</span></li>)}</ul></main>}
